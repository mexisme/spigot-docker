{
    "variables": {
        "version": "1.11.2",
        "spigot-url": "https://hub.spigotmc.org/jenkins/job/BuildTools/lastSuccessfulBuild/artifact/target/BuildTools.jar",
        "cwd": "{{env `PWD`}}",
        "local-artefact-dir": "artefacts",
        "container-dir": "/minecraft",
        "user": "minecraft",
        "uid": "10000",
        "group": "minecraft",
        "gid": "10000"
    },
    "builders": [
        {
            "type": "docker",
            "image": "alpine:3.5",
            "pull": false,
            "discard": true,
            "run_command": [ "-d", "-i", "-t", "{{.Image}}", "/bin/sh" ],
            "changes": [ "USER {{user `user`}}" ]
        }
    ],
    "provisioners": [
        {
            "type": "shell",
            "inline": [
                "apk --no-cache --update upgrade",
                "apk --no-cache add ca-certificates wget openjdk8 git bash",
                "update-ca-certificates --fresh",
                "addgroup -S -g {{user `gid`}} {{user `group`}} && adduser -S -u {{user `uid`}} -D -G {{user `group`}} -h {{user `container-dir`}} -s /bin/bash {{user `user`}}",
                "cd {{user `container-dir`}}",
                "wget {{user `spigot-url`}}",
                "su -s /bin/bash -c 'java -jar BuildTools.jar' {{user `user`}}"
            ]
        },
        {
            "type": "shell-local",
            "command": "mkdir -p {{user `local-artefact-dir`}}"
        },
        {
            "type": "file",
            "direction": "download",
            "source": "{{user `container-dir`}}/spigot-{{user `version`}}.jar",
            "destination": "./{{user `local-artefact-dir`}}/"
        },
        {
            "type": "file",
            "direction": "download",
            "source": "{{user `container-dir`}}/BuildTools.log.txt",
            "destination": "./{{user `local-artefact-dir`}}/"
        }
    ]
}
